name: Integration Tests

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'e2e/docker-compose.test.yml'
      - 'Dockerfile'
      - 'e2e/Dockerfile.mock-vault'
      - 'e2e/config.test.yaml'
      - 'e2e/test-integration.sh'
      - '.github/workflows/integration-tests.yml'
  pull_request:
    branches:
      - main
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'e2e/docker-compose.test.yml'
      - 'Dockerfile'
      - 'e2e/Dockerfile.mock-vault'
      - 'e2e/config.test.yaml'
      - 'e2e/test-integration.sh'
      - '.github/workflows/integration-tests.yml'

permissions:
  contents: read

jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install integration test tools
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Run integration tests
        run: |
          chmod +x ./e2e/test-integration.sh
          ./e2e/test-integration.sh

      - name: Collect Docker logs
        if: failure()
        run: |
          docker compose -f e2e/docker-compose.test.yml ps
          docker compose -f e2e/docker-compose.test.yml logs --no-color

      - name: Cleanup containers
        if: always()
        run: docker compose -f e2e/docker-compose.test.yml down -v
